includes:
    - vendor/phpstan/phpstan-strict-rules/rules.neon

parameters:
    level: max

    paths:
        - src
        - tests

    phpVersion: 80500

    tmpDir: var/phpstan

    bootstrapFiles:
        - vendor/autoload.php

    reportUnmatchedIgnoredErrors: true
